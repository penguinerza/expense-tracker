# Currency & formatting (build-time, baked into the bundle).
# Defaults to Japanese Yen if unset. Intl handles the symbol, grouping,
# and decimal places per currency automatically.
#   USD → $1,234.56   EUR → 1.234,56 €   JPY → ¥1,235
VITE_CURRENCY=JPY
VITE_LOCALE=ja-JP
